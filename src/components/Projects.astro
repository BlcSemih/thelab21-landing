---
const techTags = ['Next.js', 'TypeScript', 'Tailwind CSS', 'Firebase'];
---

<section id="projekte" class="py-24 px-6">
  <div class="max-w-6xl mx-auto">
    <h2 class="section-title text-center mb-16">Unsere Projekte</h2>
    
    <div class="max-w-3xl mx-auto">
      <div class="card p-8">
        <div class="flex flex-col md:flex-row gap-8">
          <!-- Project Info -->
          <div class="flex-1">
            <h3 class="text-2xl font-bold text-foreground mb-3">AP1-Trainer</h3>
            <p class="text-muted mb-6">
              Lernplattform für die IHK AP1 Prüfung. Über 1200 Fragen, 38 Themen, Lernkarten und Prüfungssimulation.
            </p>
            
            <!-- Tech Tags -->
            <div class="flex flex-wrap gap-2 mb-6">
              {techTags.map((tag) => (
                <span class="px-3 py-1 text-sm text-muted bg-white/5 border border-border rounded-full">
                  {tag}
                </span>
              ))}
            </div>
            
            <a 
              href="https://app.ap1-trainer.de" 
              target="_blank" 
              rel="noopener noreferrer"
              class="btn-primary inline-flex items-center gap-2"
            >
              Projekt ansehen
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
              </svg>
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
